#include <iostream>
#include <unistd.h>

int main(int argc, char * argv[] ) {

        for(;;) {

                std::cout << "HOla mundo" << std::endl;
                sleep(1);
        }

        return 0;

}
